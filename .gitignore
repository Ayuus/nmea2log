__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/
.geocode_cache.json
.weather_cache.json
.marine_cache.json
.ebl_sample_cache.pkl
.trip_cache.pkl
.nmea2log.lock
logbook.csv
logbook.gpx
logbook.html
nmea2log.log
.claude/

# local W2K-2 download configuration/data -- contains credentials, must never go into git
nmea2log.ini
Actisense/
downloads/

# optional local overrides for nmea2log.bat/nmea2log-no-download.bat (e.g. VIEW_URL) -- see the
# rem comment in those files themselves; the same idea as nmea2log.ini, but for .bat-only
# settings that have no ConfigParser section.
nmea2log-local.bat

# keep locally only: not in git and not on the live site
docs/autopilot-analysis.html
docs/autopilot-analysis.pdf

